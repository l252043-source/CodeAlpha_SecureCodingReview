#pragma once
#include <raylib.h>

namespace Layout
{
    const int SCREEN_W = 1600;
    const int SCREEN_H = 900;
    const int CART_X = 1100;
    const int CART_Y = 80;
    const int CART_W = 500;
    const int CART_H = 820;
}

namespace Theme
{
    const Color BACKGROUND = { 245, 240, 230, 255 };
    const Color PRIMARY = { 212, 175,  55, 255 };
    const Color SECONDARY = { 255, 255, 255, 255 };
    const Color DARK = { 25,  25,  25, 255 };
    const Color CARD = { 250, 250, 250, 255 };
    const Color HOVER = { 230, 200, 100, 255 };
    const Color TEXT = { 40,  30,  20, 255 };
}